name: Selenium Script with Docker

on:
  workflow_dispatch:  # Trigger manually

jobs:
  selenium-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker
      run: |
        docker build -t my-selenium-app .
        docker run --rm my-selenium-app
